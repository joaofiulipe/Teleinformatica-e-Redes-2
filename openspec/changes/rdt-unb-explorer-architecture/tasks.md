## 1. Estrutura de diretórios

- [x] 1.1 Criar os diretórios `src/`, `tarefas/`, `capturas/tarefa0/` e `docs/` na raiz do projeto e verificar com `find`/`ls` que a árvore corresponde à descrita em `design.md`
- [x] 1.2 Adicionar um `.gitkeep` (ou equivalente) em `capturas/tarefa0/` para o diretório ser versionado antes de existirem arquivos de captura, e verificar que `git status` mostra o diretório rastreável

## 2. Módulo de transporte (`src/transporte.py`) — capability `rdt-unb/transporte`

- [x] 2.1 Criar apenas os arquivos .py. como por exemplo src/transporte.py e os outros necessarios

